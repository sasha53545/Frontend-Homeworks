/**
 * This method should return index of element in the array
 *
 * In the case of wrong data or if element is not in the array method returns -1
 *
 * This method may have complexity O(n)
 *
 * @param arr
 * @param item
 * @return {undefined|*}
 */
export const linearSearch = (arr, item) => {
};