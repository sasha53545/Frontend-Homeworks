/**
 * This method should return index of item in the array or -1 if it is not possible
 *
 * Complexity of this method should no rise faster that O(n * log(n)) !
 *
 * @param arr
 * @param item
 */
export const binarySearch = (arr, item) => {
};
