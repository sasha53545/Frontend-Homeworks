import {LLNode} from './LLNode';

export class LinkedList {
    /**
     * @type {LLNode}
     */
    root = null;

    constructor(root = null) {
        this.root = root;
    }

    length() {
    }

    push(data) {
    }
}
